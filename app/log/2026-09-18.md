# 作業ログ: 2026-09-18

## 作業概要
- アプリ開発の基本方針策定と開発ルールの設定
- 基本ゲーム進行（ゲームサイクル・スケジュール）の仕様定義
- app開発専用のログ運用およびHANDOFF引き継ぎルールの整備

## 決定・実施事項
1. **GEMINI.md（プロジェクトインストラクション）への開発ルール追記**:
   - app配下で作業を行う際は `app/log/YYYY-MM-DD.md` に日毎の記録を残すこと。
   - それとは別に `app/log/HANDOFF.md` に最新ステータスと引き継ぎメモを常時記載・更新すること。
2. **FEATURES.mdへの基本ゲーム進行仕様の追記**:
   - 28日間（月曜始まり・4週間）で28日目にエンディングを迎えるゲームサイクル。
   - 1日のタイムライン：
     - 朝: 登校イベント等の固定イベント（1回）
     - 午前: 行動ターン（固定選択肢から場所選択、事前情報あり）
     - 昼: 行動ターン（固定選択肢から場所選択、事前情報あり）
     - 放課後: 行動ターン（固定選択肢から場所選択、事前情報あり）
     - 夜: 自室（セーブ、ロード、1日をやり直す、就寝）
   - 例外・強制イベント: 日中に出会い救済等の強制イベントが発生することがある。
3. **作業ログ基盤の作成**:
   - `app/log/` ディレクトリ作成
   - `app/log/HANDOFF.md` 初期作成
   - `app/log/2026-09-18.md` 本日の作業ログ作成
4. **最低限機能の開発タスク一覧（Task.md）の策定**:
   - `app/Task.md` を作成。
   - プロジェクト基盤、ゲームループ（28日間）、会話システム、3D・ビジュアル描画、セーブ/ロード、UI・画面フロー、テストの7分類でMarkdownチェックボックス形式にてタスク化。
5. **シナリオ設計・テスト方針および多言語統合設計の反映**:
   - `app/TECH_STACK.md`, `app/Task.md`, `app/FEATURES.md` を更新。
   - **シナリオ単体テスト容易性**: 画面（React/Three.js/DOM）に依存しない純粋なHeadlessステートマシンとしてシナリオエンジンを設計し、Vitest等で全シーン遷移・フラグ制御・バリデーションを自動単体テスト可能に。
   - **多言語統合データ構造**: 既存の上位階層のように言語ごとに別インスタンスで二重管理するのを廃止し、1つのシナリオインスタンス内で `text: { ja, en }` / `text_ja`/`text_en` または辞書参照を持たせる統合構造を採用。

6. **プロジェクト基盤・環境構築および基本型定義の実装 (Task.md セクション1完了)**:
   - `app/package.json`, `app/vite.config.ts`, `app/tsconfig.json`, `app/index.html` を作成し独立ビルド構成を確立。
   - `npm install` により React 19, Three.js, @pixiv/three-vrm, Vitest を導入。
   - 基本型定義を作成：
     - `app/src/types/game.ts`: 28日サイクル、時間帯フェーズ（朝/午前/昼/放課後/夜）、フラグ、好感度、場所選択定義
     - `app/src/types/scenario.ts`: 単一インスタンス多言語構造（`text: { ja, en }`、日本語ボイス専用）、選択肢、シーン
     - `app/src/types/save.ts`: localStorage保存・1日やり直しバックアップ型定義
7. **Headless シナリオエンジン & 単体テスト基盤の実装 (Task.md セクション3/7一部完了)**:
   - `app/src/services/scenario/ScenarioEngine.ts`: 画面非依存の進行ステートマシン。テキスト送り、選択肢分岐、gotoジャンプ、フラグ更新、好感度加算を実装。
   - `app/src/services/scenario/ScenarioValidator.ts`: 重複IDや不正gotoを検出する静的バリデータ。
   - `app/src/scenarios/sampleScenario.ts`: 多言語サンプルシナリオ定義。
   - `app/src/services/scenario/__tests__/ScenarioEngine.test.ts`: Vitest による6件の単体テストを作成し全てPASS（実行時間0.2秒）。
8. **Reactエントリ & プロトタイプ画面の実装**:
   - `app/src/App.tsx`, `main.tsx`, `index.css`: 日時・フェーズヘッダー表示、言語切り替え（JA/EN）、会話送り、選択肢クリック、夜の自室メニュー（セーブ/ロード/1日をやり直す/就寝）の動作モックを構築。
   - `npm run build` がエラーゼロで成功することを確認。

9. **ゲームループ・スケジュール管理（28日間サイクル）の本格実装 (Task.md セクション2完了)**:
   - `app/src/data/characters.ts`: キャラクターマスタ（葵、紫苑、エミリの多言語名・説明・モデルパス・テーマカラー）を定義。
   - `app/src/data/locations.ts`: 行動場所マスタ（教室、中庭、屋上、図書室、購買、運動場等）を定義。
   - `app/src/services/schedule/ScheduleManager.ts`:
     - 朝イベント選定（Day 1初日・通常日）
     - 行動ターン（午前・昼・放課後）における場所事前情報（滞在キャラヒント・ヒント文）生成
     - 日中強制イベント判定（未遭遇キャラ救済: Day 2午前紫苑、Day 3昼エミリ等）
     - 行動場所別シナリオ解決
     - 1日のフェーズ順次進行（朝 → 午前 → 昼 → 放課後 → 夜 → 翌朝）
     - 「1日をやり直す」による当日朝への巻き戻し
     - 28日目終了時のエンディング到達判定および好感度別エンディング分岐（葵エンド/紫苑エンド/ノーマルエンド）
   - シナリオ拡充:
     - `app/src/scenarios/morningScenarios.ts`: 朝登校イベント
     - `app/src/scenarios/actionScenarios.ts`: 教室、図書室、屋上等の会話・選択肢
     - `app/src/scenarios/forcedScenarios.ts`: 廊下での衝突、迷子案内等の強制イベント
     - `app/src/scenarios/endingScenarios.ts`: 各種エンディングシナリオ

10. **セーブ・ロードサービス (SaveService) の実装 (Task.md セクション5完了)**:
    - `app/src/services/save/SaveService.ts`: localStorage へのゲーム進行状態・当日朝スナップショット保存、読み込み、削除、JSON破損時のフォールバック。
    - 単体テスト `SaveService.test.ts` で4件のテスト作成・合格。

11. **UIコンポーネント分割 & メインループ統合 (Task.md セクション3/6完了)**:
    - `src/components/Header/GameHeader.tsx`: 日付・曜日・フェーズ・言語切り替え
    - `src/components/ActionSelect/ActionSelectModal.tsx`: 場所選択＆事前ヒントカードグリッド
    - `src/components/Dialogue/DialogueBox.tsx`: メッセージウィンドウ・クリック送り
    - `src/components/Dialogue/ChoiceBox.tsx`: 選択肢ボタン群
    - `src/components/Room/NightRoomView.tsx`: 自室メニュー（好感度パネル、セーブ/ロード/やり直し/就寝）
    - `src/components/Ending/EndingView.tsx`: 28日完走エンディング画面
    - `src/styles/index.css`: 場所選択・自室・エンディング等のスタイル定義

12. **自動テスト & Playwright E2Eブラウザ検証 (Task.md セクション7完了)**:
    - Vitest単体・結合テスト（全23件全PASS、実行時間約0.2秒）:
      - シナリオエンジン、静的バリデータ
      - セーブ・ロード・当日朝バックアップ
      - スケジュールマネージャ
      - 28日間コアループ結合テスト（Day 1朝〜夜〜就寝〜Day 2朝、やり直し復元、エンディング到達）
    - Playwrightブラウザ自動検証（`app/scripts/verify_gameloop.mjs`）:
      - ヘッダー表示（Day 1 朝）
      - 朝登校会話クリック進行
      - 午前行動の場所選択モーダル表示（6箇所・葵ヒント確認）
      - 教室選択 → 葵との会話 → 選択肢表示 → 選択肢クリック進行
      - 昼（図書室）→ 放課後（屋上）の巡回完了
      - 夜の自室メニュー（好感度パネル、各コマンドボタン）到達を確認しスクリーンショット保存

13. **3D・ビジュアル描画システム（Three.js / VRM / 多層背景）の実装 (Task.md セクション4完了)**:
    - **時間帯（TimeOfDay）とロケーション（Location）の直交・完全分離設計**:
      - `morning_school` 等の合体プリセットを完全排除。
      - `app/src/types/visual.ts`: 時間帯パラメータ（ライト・ポストプロセス・フォグ）とロケーションパラメータ（多層背景）の型定義。
      - `app/src/data/timeOfDayPresets.ts`: ルートのチューニング値をベースとした朝 (`morning`), 昼 (`day`), 夕方 (`evening`), 夜 (`night`) の独立ライティング・ポストプロセスプリセット。
      - `app/src/data/locationVisualPresets.ts`: 各ロケーション（教室、校門、屋上、中庭、図書室、運動場、購買、自室、通学路）の純粋な多層背景（AVIF 遠景/中景/近景）プリセット。
    - **ポストプロセスUber Shader**:
      - `app/src/services/graphics/CinematicAnimeShader.ts`: 単一パスでディフュージョン（アニメグロー）、カラーグレーディング（スプリットトーニング/S字カーブ/ガンマ）、ビネット、色収差、シャープネスを実行するウーバーシェーダーを構築。
    - **Three.js & VRM コア (`StageManager.ts`)**:
      - Three.js シーン、PerspectiveCamera（歪みの少ないFOV 32度、ウェストアップ・バストアップ構図）、WebGLRenderer、EffectComposer、ライト、フォグを管理。
      - 多層背景メッシュ制御（遠景: `scene.background`、中景: `renderOrder = -1`、近景: `renderOrder = 2`）。
      - `@pixiv/three-vrm` によるVRM非同期ロード、キャッシュ、キャラクター切り替え、表情（モーフウェイト 1.0 原則）、自然な腕下ろし立ちポーズの初期適用。
    - **React統合 (`StageView.tsx`, `App.tsx`)**:
      - ResizeObserver による自動リサイズ追従。
      - 現在の進行フェーズから `activeTimeOfDay`、選択場所やシナリオから `activeLocationId`、シーンから話者アバターと表情を自動算出してステージに反映。
    - **動作確認・ブラウザ自動検証**:
      - `npm run build`: TypeScript型チェック & Vite本番ビルド成功。
      - `npm run test`: Vitest単体・結合テスト全23件 PASS（0.25秒）。
      - Playwright実ブラウザ自動検証（`app/scripts/verify_stage_visuals.mjs`）: 朝校門＋葵立ち絵、セリフ進行による笑顔モーフ反映、午前教室選択による教室背景＋葵の切り替えを実機描画で確認し、エラーゼロ（0 errors）でスクリーンショット取得・検証完了。

14. **ルートリポジトリ描画スタックの完全移植・ビジュアル強化**:
    - **プロシージャル空・雲描画 (`SkyBackground.ts`)**:
      - アルファ透過背景画像（校門・教室等の空が抜かれたAVIF画像）の背後に、ShaderMaterialによるプロシージャル青空・流れる雲（FBMノイズ）・太陽コア・ハローを合成描画。空が黒く抜ける問題を完全に解消。
      - `setTimeOfDay` 連動により、朝・昼・夕・夜の空と雲の色調、空グローがリアルタイムに変化。
    - **完全ポストプロセスパイプライン構築**:
      - `RenderPass` ➔ `UnrealBloomPass` ➔ `GodRaysPass` (GodRaysShader) ➔ `CinematicAnimeShader` ➔ `SMAAPass` ➔ `OutputPass` を構築。
      - `SunEffect.ts` & `OcclusionRaycaster.ts`: 太陽フレア、レンズゴースト、スターバースト、VRMアバター遮蔽時の光条自動減衰オクルージョン判定を移植。
    - **MToonマテリアルのアニメ調シェーダーパッチ (`ToonShader.ts`, `SmoothNormalHelper.ts`)**:
      - 影の色相シフト（青紫・茜色）、顔の影閾値補正（`faceShadingShiftFactor`）、陰影硬度、法線スムージングによる繊細なアウトライン、パラメトリックリムライトを適用。
    - **FBXモーションアニメーション & 表情スムーズ遷移 (`Avatar.ts`)**:
      - `FBXLoader` + `loadMixamoAnimation` リターゲティングによる待機モーション（`Standing Idle.fbx`）の常時ループ再生。
      - 表情の指数減衰クロスフェード補間（duration = 0.25s）により、一瞬での切り替えではなく自然で滑らかな表情変化を実現。
      - 自然なランダム間隔での自動まばたき（`autoBlink`）を実装。
    - **実機自動検証 (Playwright)**:
      - 朝校門前および午前教室での美麗な空・雲、サンシャフト、自然な呼吸待機モーション、滑らかな笑顔クロスフェード、ToonShaderマテリアル描画を確認し、コンソールエラー 0 件でスクリーンショットを保存・検証完了。

15. **音楽・音声・リップシンク（AudioLipSync / SoundManager）の移植と完全統合 (Task.md セクション8完了)**:
    - **アセット配置**:
      - `public/wasm/lipsync.wasm`, `public/worklets/lipsync-processor.js`
      - `public/bgm/` (`thema_music.mp3`, `bgm.mp3`, `Waiting_Beneath_the_Boards.mp3`)
      - `public/se/` (`walking.mp3`, `door_chime.mp3`, `items_chose.mp3` 等)
      - `public/voices/` (`001.wav`, `chat_intro_1.wav`, `chat_park_1.wav` 等)
    - **オーディオサービス実装 (`AudioLipSync.ts`, `SoundManager.ts`)**:
      - `AudioLipSync.ts`: Web Audio API + AudioWorklet + WebAssembly によるリアルタイム母音解析（`aa`, `ee`, `ih`, `oh`, `ou`）。Node環境でのテスト実行を考慮した安全なフォールバック設計。
      - `SoundManager.ts`: BGMループ再生（同曲継続判定、クロスフェード配慮）、SEワンショット再生、マスター/BGM/SE音量制御。
    - **アバター・ステージ連動 (`Avatar.ts`, `StageManager.ts`)**:
      - `Avatar.updateLipSync()`: VRM ExpressionManager の母音モーフ（`aa`, `ee`, `ih`, `oh`, `ou`）へ高速アタック＆滑らか減衰で重みを反映。
      - `StageManager.ts`: レンダリングループ内でアクティブアバターに `audioLipSync.currentPhoneme` をリアルタイム反映。
    - **シナリオ・App統合 (`morningScenarios.ts`, `App.tsx`, `StageView.tsx`)**:
      - シーン定義の `bgmUrl`, `seUrl`, `voiceUrl` を進行に合わせて自動再生。
      - 最初のユーザー操作（画面クリック）で安全にオーディオアンロック。
    - **テスト・実機自動検証**:
      - `npm run test`: Vitest 全25件 PASS（オーディオサービステスト追加）。
      - `npm run build`: 型エラー0・バンドル成功。
      - `scripts/verify_audio_lipsync.mjs`: Playwrightによるブラウザ検証で朝シーンBGM再生、セリフ進行に伴うボイス再生およびアバターの口開閉リップシンク描画を確認（`audio_stage_speaking.png`）。エラーゼロで完了。

## 次回作業
- **Task.md セクション6「タイトル画面・ローディング表示」の実装**:
  - タイトル画面（はじめから / つづきから）
  - アセットロード中のインジケータ表示
  - 操作案内・ヘルプ

