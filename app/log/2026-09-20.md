# 2026-09-20 作業ログ

## 1. 目的
`app/` 配下のアンチエイリアス処理（MSAA・SMAA・解像度スケーリング）をルートアプリ（`src/scene/ViewerCore.ts`）の描画パイプラインと一致させ、ジャギーの低減と描画品質の統一を図る。

## 2. 変更要点
- **`app/src/services/graphics/StageManager.ts`**:
  - `EffectComposer` 初期化時に明示的な `WebGLRenderTarget`（`samples: 4`、`type: THREE.HalfFloatType`）を渡すよう修正（MSAA有効化・HDR品質維持）。
  - `WebGLRenderer` の `antialias` を `false` に変更（ポストプロセス時はオフスクリーンターゲット側でMSAAを処理するため）。
  - `this.composer.setPixelRatio(pr)` を設定し、`resize` 時に実ピクセル解像度（Retinaスケーリング）で `smaaPass.setSize` および `cinematicAnimePass.uniforms['uResolution']` を更新するよう修正。
  - `dispose` 時にコンポーザーのレンダーターゲット破棄処理を追加。

## 3. 動作確認結果
- `npm run build`: 正常終了（ライセンス自動生成、型チェック、Viteビルド成功）。
- `npm run test`: 全7テストファイル・34件のテストすべて成功。
