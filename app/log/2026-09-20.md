# 2026-09-20 作業ログ

## セーブ・ロード画面のデザイン規約準拠・レイアウト改善

### 1. 目的
- UI規則（絵文字禁止・表記シンプル化）への完全準拠
- セーブ・ロードスロットの視覚的統一と情報配置の最適化

### 2. 主な変更点
- **タイトル表示の規約準拠**:
  - タイトル先頭のアイコンバッジを削除
  - タイトルの括弧つき補足（`（進行状況の記録）`、`（再開データの選択）`）を削除し、`セーブ` / `ロード` に統一
- **親密度表示の洗練・ラベル化**:
  - 名前テキストや丸ドットを排し、レベルラベル（`Lv.X`）のみを横並びに配置
  - プロジェクト規約のテーマカラー（アオイ: 黄色、エミリ: 赤、シオン: 青）をバッジ色に厳格適用
- **未合意フィールド（shortDescription / description）の完全削除**:
  - `CharacterMaster` 型定義および `characters.ts` から未合意の人物設定（`shortDescription`）を削除
  - `ActionLocationOption` 型定義および `locations.ts` から未合意の場所説明（`description`）を削除
  - `BgmPreset` 型定義および `bgmPresets.ts` から未合意の楽曲説明（`description`）を削除
- **プロジェクト開発規則へのテーマカラー明記**:
  - `GEMINI.md` に各キャラクターのテーマカラー規則（アオイ=黄色、エミリ=赤、シオン=青）を追加明記
  - `characters.ts` の定義カラーを規約通りに統一

### 3. 確認結果
- 単体テスト（全35件）・プロダクションビルドが正常に通過することを確認
- 実機プレビューによるスクリーンショット撮影（セーブ画面・ロード画面）を実施し、表示デザインを確認

---

## 画面ロジック・ヒント情報・エンディング仕様のリファクタリング

### 1. 目的
- `ScheduleManager.ts` に散在していた行動ヒント情報のシナリオ側集約
- 1000行超に肥大化していた `App.tsx` の `pages/` ディレクトリへの画面分割
- 未合意だったエンディング分岐処理（`getEndingScenario`）の排除とTODO化

### 2. 主な変更点
- **ヒント情報の `ScenarioPackage` 側集約**:
  - `ScenarioPackage` に `actionHints` フィールドを追加
  - 各行動シナリオパッケージ（`actionScenarios.ts`）に場所・フェーズ・ヒント文・登場キャラIDを定義
  - `ScheduleManager` はシナリオ定義からヒント情報・対応シナリオを解決するように変更
- **`App.tsx` の画面分割（`pages/`）**:
  - `TitlePage`: タイトル画面の表示・開始・再開・言語・ミュート
  - `ScenarioPage`: 3Dステージ・会話・選択肢・オート進行
  - `ActionSelectPage`: 場所選択画面（マップ・候補一覧）
  - `NightRoomPage`: 夜の自室画面（セーブ・ロード・やり直し・就寝）
  - `EndingPage`: 28日目完走画面（結果表示・タイトル戻り、絵文字排除）
  - `App.tsx` は画面ルーティングと共通モーダル・サービスの管理に集約し、約600行へスリム化
- **未合意エンディング仕様の排除**:
  - `ScheduleManager` の好感度判定・個別エンディングシナリオ分岐（`getEndingScenario`）を完全削除し「TODO: エンディング仕様確定後に実装」とコメント記載
  - 28日目満了時はそのまま `EndingPage` へ遷移するように整理

### 3. 確認結果
- 単体テスト・コアループ結合テスト全34件が正常通過
- プロダクションビルド（Vite build / TypeScript noEmit）正常完了
- Playwright E2Eによるローディング画面 → タイトル画面 → シナリオ画面への画面遷移・描画検証が正常通過（エラー0件）

