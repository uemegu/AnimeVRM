# AnimeVRM プロジェクト開発ガイドライン & インストラクション

## 画像アセット変換 (`avifenc`)

プロジェクト内で使用する背景やテクスチャ画像は、パフォーマンスと軽量化のため **AVIF形式 (`.avif`)** を標準として採用します。画像を追加・更新する際は `avifenc` を使用して変換してください。

### 変換コマンド

1. **通常画像（背景・遠景・不透明テクスチャ）**:
   ```bash
   avifenc -s 6 -q 85 input.png output.avif
   ```

2. **透過画像（中景・近景・アルファチャンネル付きテクスチャ）**:
   ```bash
   avifenc -s 6 -q 85 --qalpha 100 input.png output.avif
   ```
   > `--qalpha 100` を指定することで、透過部分の境界や半透明グラデーションが劣化せず完全ロスレスで維持されます。

---

## 多層背景設計 (Background / Midground / Nearground)

- **遠景 (Background)**: `scene.background` に設定。画面全体に広がる背景。
- **中景 (Midground)**: `ViewerCore.midgroundMesh`。アバターより奥（`renderOrder = -1`）に配置される環境オブジェクト（公園の樹木など）。
- **近景 (Nearground)**: `ViewerCore.neargroundMesh`。アバターより手前（`renderOrder = 2`）に配置される前景オブジェクト（カフェのテーブルなど）。アバターを挟み込むことで「座っている」「奥に立っている」シチュエーションを表現する。

---

## ブラウザ検証・ヘッドレステスト (`npx playwright`)

ブラウザ上での描画検証やUIテストを行う際は、直接バイナリを叩くのではなく **`npx playwright`** を使用してください。
プロジェクト内に永続インストールされていない場合でも `npx` を介して一貫した実行が可能です。

### デバッグ・検証中は音を鳴らさない

AIエージェントによるデバッグ・動作確認では、BGM・SE・ボイスを実際に鳴らさないこと（開発者の作業環境で突然音が出るのを防ぐため）。

- **Playwright**: ブラウザ起動時に `--mute-audio` を付ける（例: `chromium.launch({ args: ['--mute-audio'] })`）。
- **アプリ内ブラウザ等、ブラウザ側でミュートできない場合**:
  - `app/` はページ読み込み前に `localStorage.setItem('galgame_audio_muted', 'true')` を設定してミュート状態で起動し、検証中にミュートを解除しない。
  - ルート (`src/`) は全体のミュート手段がないため、音が出る操作の検証は Playwright で行う。
- **音の再生を確かめたいとき**は、実際に鳴らさず `HTMLMediaElement.prototype.play` などを差し替えて呼び出しの有無・回数で判定する。

---

## シナリオ設計規則 (Scenario Design Guidelines)

- **選択肢シーンにおけるセリフ・ナレーションの完全排除**:
  - 選択肢（`choices`）を提示するシーンでは、セリフ（`text`）やモノローグ・ナレーションを一切含めないこと（`text: ''`）。
  - 会話文やナレーションと選択肢ボタン（および Thinking Time カウントダウンUI）が画面上に同時に存在すると、プレイヤーの認知負荷が高まりテキストも選択肢も読めなくなります。
  - 発話や感情セリフは必ず直前のシーン（例: アバターが感情をぶつけるシーン）で完結させ、次のシーンを「選択肢専用シーン」として独立させてください。

---

## 表情ウェイトの指定規則 (Expression Weight Guidelines)

- **`neutral`, `happy`, `relax` などで中途半端な値を指定しないこと**:
  - `expressionWeight` に `0.7` や `0.8` といった中途半端な数値を指定すると、表情ブレンドが中途半端・半開きになり不自然な表情になります。
  - 表情を適用する際は基本的に **`1.0`**（解除・ブレンドなし時は `0.0`）を明確に指定してください。

---

## カメラワーク・ズーム規則 (Camera & Distance Guidelines)

- **極端な接写・過度なクローズアップの禁止**:
  - 対象（アバターの顔や首元など）に極端に近づきすぎるカメラ設定（`speaker_extreme_close` や極端に短いカメラ距離）は原則禁止とします。
  - VRMモデルのテクスチャ解像度やポリゴンの粗さが目立ち、描画クオリティが損なわれるためです。
  - 表情や感情を強調する場合でも、適度な距離感（バストアップ〜ウェストアップが収まる程度）を維持してください。
- **2人・全体の構図（2ショット・引きショット）の積極的導入**:
  - 1人ずつのバストアップばかりを連続で切り替えると構図が単調で窮屈になります。
  - 会話の節目や掛け合い、互いの反応を見せる場面では、2人が同時に画面に収まる `wide` や `medium` の2ショットを定期的に織り交ぜてください。

---

## UI絵文字使用の禁止規則 (No Emojis in UI without Explicit Instruction)

- **指示のない限り絵文字をUIに使わない**:
  - ユーザーから明示的な指示がない限り、UI（ヘッダー、ボタン、モーダル、見出し、バッジ、ステータス表示など）に絵文字（📅, 🌅, 📍, 💡, 🌙, ❤️, 🛏️, 💾, 📂, ⏪ など）を使用しないこと。
  - アイコンが必要な場合は、SVGアイコンまたはテキストによる洗練されたタイポグラフィ表現を用いること。

---

## モーション選定規則 (Motion Selection Guidelines)


- **`Female Standing Pose.fbx` の使用禁止**:
  - `Female Standing Pose.fbx` はポーズや姿勢が不自然であるため、真面目な文脈や標準的な立ち姿としては使用しないでください。
  - 立ち待機モーションには `Standing Idle.fbx` や `Idle.fbx` などを標準として使用してください。

---

## 技術的限界の早期判断と報告規則 (Technical Feasibility & Early Reporting)

- **実現困難・不可能な要件に対する早期の「無理」の申告**:
  - ライブラリやプラットフォームの仕様制限（例: VRM SpringBoneの簡易バネ物理によるメッシュ貫通の完全防止の限界など）やモデル構造の制約により、根本的な対応が極めて困難または原理的に無理と判断される場合は、**無駄に試行錯誤を繰り返してトークンやユーザーの時間を浪費しないこと**。
  - できるかのように見せかけて無理な調整を繰り返すのではなく、「何が原理的に無理なのか」「なぜスクリプトやパラメータ調整のレベルでは限界があるのか」という客観的な技術的理由・限界を速やかに整理し、**「無理である」と率直に申告すること**。
  - 本質的な解決に別アプローチ（例: モデル側のメッシュ削除、手付けアニメーション、専用エンジンの導入など）が必要な場合は、代替手段や判断材料を簡潔に提示してユーザーの指示を仰ぐこと。

---

## app開発作業時の作業ログ・引き継ぎ・UI規則 (app開発限定ルール)

`app/` ディレクトリ配下での開発・実装作業を行う際は、以下のルールを厳守してください（※将来の他リポジトリ移行を前提としたapp限定ルールです）。

1. **UIデザインは白（ライトテーマ）を基調とすること**:
   - モーダル、サイドバー、ダイアログ、選択肢、パネル、カードなどのUIコンポーネントは、原則として **白（`#ffffff`、明るい半透明ガラスモーフィズム等）を基調** とした明るく洗練されたトーンで設計すること。勝手な推測や自己判断で黒・ダークテーマを適用しないこと。
   - テキストは視認性の高いダークスレート（`#0f172a` や `#334155` 等）、アクセントカラーには爽やかなブルーや指定のアクセントを用いること。
2. **日毎の作業ログ記録 (`app/log/YYYY-MM-DD.md`)**:
   - app開発作業を行う際は、`app/log/` ディレクトリ内に日毎の作業ファイル（例: `2026-09-18.md`）を作成し、その日の作業内容、決定事項、動作確認結果などを記録すること。
   - **簡潔な記録の徹底**: 細かなCSS数値、ピクセル値、カラーコード、内部変数など「コードを見ればわかる細部」は書かないこと。後から見て意味のない情報を省き、目的・変更要点・決定事項のみを端的にまとめること。
3. **引き継ぎメモの常時更新 (`app/log/HANDOFF.md`)**:
   - 日毎ログとは別に、`app/log/HANDOFF.md` を作成・更新すること。
   - 次回作業時や別のセッションへスムーズに引き継げるよう、「現在の実装状況」「直近のNext Action」「設計上の留意事項」を要点のみ簡潔に保つこと（長大化させない）。
4. **デザイン先行確認・承認規則 (Design Approval Before Testing)**:
   - UIコンポーネントや画面デザインの変更・新設を行う際は、自動テスト（E2E検証など）を実行する前に、まず画面キャプチャ等を提示してユーザーにデザインの確認・承認を得ること。ユーザーの承認を得た後にテストや次の工程へ進むこと。
5. **並びのボタン・ラベルのサイズ統一規則 (Consistency in Sizing for Inline Buttons & Badges)**:
   - 水平に並べて配置されるボタン群やステータスラベル群（ヘッダーのアクションボタン、ツールバー、バッジ等）は、パディングや文字数の微差によるガタつきや凹凸を防止するため、**明示的な固定高さ（`height`）および `box-sizing: border-box;` を指定して高さを完全に統一すること**。
   - 同列に並ぶボタン同士は、横幅（`width` または統一 `min-width`）も厳密に揃え、`justify-content: center` による中央揃えを行うことで、整然とした統一グリッドと視覚的調和を維持すること。
6. **キャラクターのテーマカラー規則 (Character Theme Colors)**:
   - **アオイ (Aoi)**: **黄色 (Yellow)**
   - **エミリ (Emili)**: **赤 (Red)**
   - **シオン (Shion)**: **青 (Blue)**
   - アバター、好感度・親密度バッジ、UI上のステータス表示、アクセントカラー等で各キャラクターを表現する際は、勝手な推測や自己判断で色を変更せず、必ず上記のテーマカラーを厳格に適用すること。

